@{
    Layout = "_Layout";
}

@using DoctorsOffice.Models;

<div class="container">
    <h1 class="jumbotron">List of Doctors</h1>

    @if (@Model.Count == 0)
    {
        <h3>No Doctors have been added yet!</h3>
    }
    else
    {
        <ul class="row">
            @foreach (Doctor doctor in Model)
            {
                <li id="doctor">@Html.ActionLink($"{doctor.Name}", "Details", new { id = doctor.DoctorId }) | Specialty: @doctor.Specialty</li>
            }  
        </ul>
        
    }

    <p class="btn btn-dark" id="link">@Html.ActionLink("Add new Doctor", "Create")</p>
</div>
